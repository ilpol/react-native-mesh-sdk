package com.bitchat.android.wifiaware

/**
 * SDK stub of bitchat's WifiAwareController.
 *
 * Wi-Fi Aware is an alternate transport the full app can toggle. The SDK ships
 * BLE-only, so enabling it is a no-op.
 */
object WifiAwareController {
    fun setEnabled(enabled: Boolean) {}
}
